describe("TODO: @repo/database/schema", () => {
  it("TODO: test invalid data", () => {
    expect(() => {
      throw new Error("Not implemented");
    }).toThrow();
  });

  it("TODO: test valid data", () => {
    expect(() => {
      void 0;
    }).not.toThrow();
  });
});
