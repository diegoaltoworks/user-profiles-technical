describe("TODO: @repo/database", () => {
  it("TODO: test stuff that should fail", () => {
    expect(() => {
      throw new Error("Not implemented");
    }).toThrow();
  });

  it("TODO: test stuff that works", () => {
    expect(() => {
      void 0;
    }).not.toThrow();
  });
});
